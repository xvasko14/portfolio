---
import Section from "../ui/Section.astro";
import SectionIntro from "../ui/SectionIntro.astro";
import { home } from "../../data/home";
---

<Section class="home-projects" id="projects-preview">
  <SectionIntro eyebrow={home.projects.eyebrow} title={home.projects.title}>
    <p>Early case-study placeholders that show the kind of platform and delivery work this portfolio will feature.</p>
  </SectionIntro>

  <div class="project-preview-grid">
    {home.projects.entries.map((item) => (
      <article class="project-preview-card">
        <h3>{item.title}</h3>
        <p>{item.summary}</p>
        <p class="project-preview-card__outcome">{item.outcome}</p>
        <a class="project-preview-card__link" href={item.href}>View project list</a>
      </article>
    ))}
  </div>
</Section>
